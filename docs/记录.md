- command interface 的 position 指"关节角度的目标值"，不是"机械臂末端的空间位姿"
- panda_hand_controller 为什么只有一个 joint、没有接口定义: panda_finger_joint2 上有一个 mimic 标签 两个手指是机械耦合的：joint1 转多少，joint2 被强制跟着转多少
- panda的world和机械臂的固定是在launch文件中直接用静态tf发布的